"use client";import{useRef}from"react";import{File,UploadCloud,X}from"lucide-react";export interface DemoFile{id:string;name:string;type:string;size:number}const accepted=".pdf,.docx,.xlsx,.dwg,.zip";const size=(bytes:number)=>bytes<1024*1024?`${Math.ceil(bytes/1024)} KB`:`${(bytes/1024/1024).toFixed(1)} MB`;
export function DemoFileSelector({files,onChange}:{files:DemoFile[];onChange:(files:DemoFile[])=>void}){const input=useRef<HTMLInputElement>(null);const add=(list:FileList|null)=>{if(!list)return;onChange([...files,...Array.from(list).map((file,index)=>({id:`${file.name}-${file.size}-${index}`,name:file.name,type:file.name.split(".").pop()?.toUpperCase()??"FILE",size:file.size}))])};return <div><div onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();add(e.dataTransfer.files)}} className="rounded-xl border-2 border-dashed bg-slate-50 p-8 text-center"><UploadCloud className="mx-auto h-8 w-8 text-slate-400"/><p className="mt-3 text-sm font-semibold text-slate-800">Drag and drop documents here</p><p className="mt-1 text-xs text-slate-500">PDF, DOCX, XLSX, DWG, or ZIP</p><button type="button" onClick={()=>input.current?.click()} className="mt-4 rounded-lg border bg-white px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Choose Files</button><input ref={input} type="file" multiple accept={accepted} onChange={e=>add(e.target.files)} className="hidden"/></div><p className="mt-3 rounded-md bg-amber-50 px-3 py-2 text-xs font-medium text-amber-800">Demo document selection — files are not uploaded or stored.</p>{files.length>0&&<ul className="mt-4 divide-y rounded-lg border">{files.map(item=><li key={item.id} className="flex items-center gap-3 p-3"><File className="h-4 w-4 text-slate-500"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{item.name}</p><p className="text-xs text-slate-500">{item.type} · {size(item.size)}</p></div><button onClick={()=>onChange(files.filter(file=>file.id!==item.id))} aria-label={`Remove ${item.name}`} className="rounded-md p-2 text-slate-500 hover:bg-slate-100"><X className="h-4 w-4"/></button></li>)}</ul>}</div>}
