import{BrainCircuit,CheckCircle2,FileUp,MailOpen,ReceiptText}from"lucide-react";import type{ActivityEvent}from"@/types";import{formatRelativeTime}from"@/lib/utils";import{Card}from"@/components/ui/card";const icons={bid:ReceiptText,scope:CheckCircle2,campaign:MailOpen,ai:BrainCircuit,project:FileUp};export function ProjectActivityFeed({events}:{events:ActivityEvent[]}){return <Card><div className="border-b px-5 py-4"><h2 className="font-semibold">Recent Project Activity</h2><p className="mt-0.5 text-xs text-slate-500">Latest updates for this project</p></div><ul className="divide-y">{events.map(event=>{const Icon=icons[event.type];return <li key={event.id} className="flex gap-3 p-4"><div className="rounded-lg bg-slate-100 p-2 text-slate-600"><Icon className="h-4 w-4"/></div><div><p className="text-sm font-medium">{event.title}</p><time dateTime={event.createdAt} className="mt-1 block text-xs text-slate-500">{formatRelativeTime(event.createdAt)}</time></div></li>})}</ul></Card>}
