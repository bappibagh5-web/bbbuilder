import{Check,Clock3}from"lucide-react";import type{ProjectWorkflowStage}from"@/types";import{cn}from"@/lib/utils";
export function ProjectWorkflow({stages}:{stages:ProjectWorkflowStage[]}){return <section aria-label="Project workflow" className="rounded-xl border bg-white p-4"><div className="mb-4 flex items-center justify-between"><h2 className="text-sm font-semibold text-slate-900">Project Workflow</h2><p className="text-xs text-slate-500">{stages.filter(s=>s.status==="complete").length} of {stages.length} stages complete</p></div><ol className="grid min-w-[780px] grid-cols-8 overflow-hidden rounded-lg border bg-slate-50 max-lg:min-w-[720px]">{stages.map((stage,index)=><li key={stage.id} className={cn("relative border-r p-3 last:border-0",stage.status==="current"&&"bg-blue-50",stage.status==="attention"&&"bg-amber-50")}><div className="flex items-center gap-2"><span className={cn("grid h-6 w-6 place-items-center rounded-full text-xs",stage.status==="complete"&&"bg-emerald-600 text-white",stage.status==="current"&&"bg-blue-700 text-white",stage.status==="upcoming"&&"border bg-white text-slate-400",stage.status==="attention"&&"bg-amber-600 text-white")}>{stage.status==="complete"?<Check className="h-3.5 w-3.5"/>:stage.status==="current"?<Clock3 className="h-3.5 w-3.5"/>:index+1}</span><span className="text-[11px] font-semibold leading-4 text-slate-700">{stage.label}</span></div><p className="mt-2 text-[10px] capitalize text-slate-500">{stage.status}</p></li>)}</ol></section>}
