import { Search } from "lucide-react";
import { projectStatusOptions } from "./production-project-status";

export function ProjectFilters({ search, onSearch, stage, onStage, type, onType, types }: { search: string; onSearch: (value: string) => void; stage: string; onStage: (value: string) => void; type: string; onType: (value: string) => void; types: string[] }) {
  return <div className="grid gap-3 border-b bg-slate-50/60 p-4 md:grid-cols-[1fr_210px_230px]"><label className="relative"><span className="sr-only">Search projects</span><Search className="absolute left-3 top-2.5 h-4 w-4 text-slate-400" /><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Search project, number, or client..." className="h-9 w-full rounded-lg border bg-white pl-9 pr-3 text-sm outline-none focus:border-slate-400" /></label><select aria-label="Filter by stage" value={stage} onChange={(event) => onStage(event.target.value)} className="h-9 rounded-lg border bg-white px-3 text-sm text-slate-700"><option value="all">All statuses</option>{projectStatusOptions.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select><select aria-label="Filter by project type" value={type} onChange={(event) => onType(event.target.value)} className="h-9 rounded-lg border bg-white px-3 text-sm text-slate-700"><option value="all">All project types</option>{types.map((item) => <option key={item}>{item}</option>)}</select></div>;
}
