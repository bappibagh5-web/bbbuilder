import{AlertTriangle,CalendarClock,CircleAlert}from"lucide-react";import type{ProjectAttentionItem}from"@/types";import{Card}from"@/components/ui/card";export function ProjectAttentionList({items,days}:{items:ProjectAttentionItem[];days:number}){const all=[...items,{id:"deadline",title:"Bid Deadline",message:`${days} days remaining`,severity:"info" as const}];return <Card><div className="border-b px-5 py-4"><h2 className="font-semibold">Requires Attention</h2><p className="mt-0.5 text-xs text-slate-500">Coverage and deadline risks</p></div><ul className="divide-y">{all.map(item=>{const Icon=item.severity==="critical"?CircleAlert:item.severity==="info"?CalendarClock:AlertTriangle;return <li key={item.id} className="flex gap-3 p-4"><div className="rounded-lg bg-slate-100 p-2 text-slate-600"><Icon className="h-4 w-4"/></div><div><p className="text-sm font-medium">{item.title}</p><p className="mt-0.5 text-xs leading-5 text-slate-500">{item.message}</p></div></li>})}</ul></Card>}
