import{ArrowUpRight,Clock3,FileCheck2,Inbox}from"lucide-react";import{Card}from"./ui/card";import{cn}from"@/lib/utils";
const icons=[Inbox,Clock3,FileCheck2,ArrowUpRight];
export function MetricCard({label,value,detail,tone,index}:{label:string;value:string;detail:string;tone:"positive"|"warning"|"neutral";index:number}){const Icon=icons[index%icons.length];return <Card className="p-5"><div className="flex items-start justify-between"><div><p className="text-sm font-medium text-slate-600">{label}</p><p className="mt-3 text-3xl font-semibold tracking-tight text-slate-900">{value}</p></div><div className="rounded-lg bg-slate-100 p-2 text-slate-600"><Icon className="h-4 w-4"/></div></div><p className={cn("mt-3 text-xs font-medium",tone==="positive"&&"text-emerald-700",tone==="warning"&&"text-amber-700",tone==="neutral"&&"text-slate-500")}>{detail}</p></Card>}
