import{FileSearch}from"lucide-react";import type{DocumentSourceReference}from"@/types";export function SourceReference({source}:{source?:DocumentSourceReference}){if(!source)return <span className="text-xs text-slate-400">No source reference</span>;return <details className="group mt-2"><summary className="inline-flex cursor-pointer list-none items-center gap-1.5 text-xs font-medium text-blue-700"><FileSearch className="h-3.5 w-3.5"/>View demo extracted source</summary><div className="mt-2 rounded-md border bg-slate-50 px-3 py-2 text-xs text-slate-600"><p className="font-medium text-slate-800">{source.documentName}</p><p className="mt-0.5">{source.sourceLabel}{source.sheetNumber?` · Sheet ${source.sheetNumber}`:""}{source.pageNumber?` · Page ${source.pageNumber}`:""}</p><p className="mt-1 text-[10px] uppercase tracking-wider text-slate-400">Demo source metadata</p></div></details>}
