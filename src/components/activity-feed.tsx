import{BrainCircuit,CheckCircle2,MailCheck,Send}from"lucide-react";import type{ActivityEvent}from"@/types";import{formatRelativeTime}from"@/lib/utils";import{Card}from"./ui/card";
const icons={bid:MailCheck,ai:BrainCircuit,scope:CheckCircle2,campaign:Send,project:CheckCircle2};
export function ActivityFeed({events}:{events:ActivityEvent[]}){return <Card><div className="border-b px-5 py-4"><h2 className="font-semibold text-slate-900">Recent Activity</h2><p className="mt-0.5 text-xs text-slate-500">Latest updates across active work</p></div><ul className="divide-y">{events.map(event=>{const Icon=icons[event.type];return <li key={event.id} className="flex gap-3 px-5 py-4"><div className="mt-0.5 rounded-lg bg-slate-100 p-2 text-slate-600"><Icon className="h-4 w-4"/></div><div className="min-w-0"><p className="text-sm font-medium text-slate-900">{event.title}</p><p className="mt-0.5 truncate text-xs text-slate-500">{event.projectName}</p><time className="mt-1 block text-[11px] text-slate-400" dateTime={event.createdAt}>{formatRelativeTime(event.createdAt)}</time></div></li>})}</ul></Card>}
