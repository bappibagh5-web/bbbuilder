import type{Metadata}from"next";import{notFound}from"next/navigation";import{getProject,getProjectDetails,projects}from"@/data";import{ProjectWorkspaceHeader}from"@/components/projects/project-workspace-header";import{ProjectWorkspaceTabs}from"@/components/projects/project-workspace-tabs";import{ProjectWorkflow}from"@/components/projects/project-workflow";
export function generateStaticParams(){return projects.map(project=>({projectId:project.id}))}export async function generateMetadata({params}:{params:Promise<{projectId:string}>}):Promise<Metadata>{const{projectId}=await params;const project=getProject(projectId);return project?{title:project.name,description:`${project.projectNumber} project workspace for ${project.client}.`,openGraph:{images:[]},twitter:{images:[]}}:{title:"Project Not Found"}}
export default async function WorkspaceLayout({children,params}:{children:React.ReactNode;params:Promise<{projectId:string}>}){const{projectId}=await params;const project=getProject(projectId);if(!project)notFound();const details=getProjectDetails(projectId);return <div className="mx-auto max-w-[1500px] space-y-5"><ProjectWorkspaceHeader project={project}/><div className="overflow-x-auto"><ProjectWorkflow stages={details.workflow}/></div><div className="rounded-xl border bg-white"><ProjectWorkspaceTabs projectId={projectId}/><div className="p-4 sm:p-5">{children}</div></div></div>}
